Bootstrap:docker
From:sigven/pcgr

%help
    Container for Personalised Cancer Genome Reporter

%post
    ##cpsr git
    cd /usr/local
    git clone https://github.com/sigven/cpsr
    cd cpsr
    git reset --hard fac9bd4b1f7ee7df85588c7c3c2507fc4ca5c430
    chmod a+x cpsr.py cpsr.toml
    ln -sf /usr/local/cpsr/cpsr.py /usr/local/bin/
    ln -sf /usr/local/cpsr/cpsr.toml /usr/local/bin/
